<%= render 'shared/page_title', title: 'showing all ingredients' %>

<div align = "center">
  <%= will_paginate @ingredients %>
</div>

<div align="center">
  <% @ingredients.each do |ingredient| %>
    <!-- <ul class="listing">
      <div class="row">
        <div class="well col-md-4 col-md-offset-4">
          <li><%= link_to ingredient.name.capitalize, ingredient_path(ingredient) %></li>
          <li><small><%= pluralize(ingredient.recipes.count, "recipe") if ingredient.recipes %></small></li>
          <% if logged_in? && current_user.admin?%>
            <li><%= link_to "Edit this ingredient", ingredient_path(ingredient)%>
            </li>
          <%end%>
        </div>
      </div>
    </ul> -->
    <div class="list-group">
      <ul class="list-group">
        <%= link_to ingredient.name.capitalize, ingredient_path(ingredient), class: "list-group-item" %>
      </ul>
    </div>
  <% end %>
</div>
