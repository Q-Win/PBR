<%= render 'shared/page_title', title: "#{@chef.name}" %>
<div class="row">
  <div class="clo-md-4 col-md-offset-4 center">
    <%= gravatar_for(@chef, size: 150) %>
  </div>
</div>
<h4 algin="center"><%= @chef.name%>'s recipes</h4>
</br>
<%= render 'recipes/recipes', obj: @recipes %>
